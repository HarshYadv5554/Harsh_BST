services:
  - type: web
    name: bst-backend
    env: node
    repo: https://github.com/HarshYadv5554/Harsh_BST.git
    branch: main
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start

  - type: static
    name: bst-frontend
    repo: https://github.com/HarshYadv5554/Harsh_BST.git
    branch: main
    rootDir: frontend
    # Use npm exec to safely run vite; no permission issues with this approach
    buildCommand: npm ci && npm exec -- vite build
    publishPath: dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_BASE_URL
        value: https://bst-backend.onrender.com
